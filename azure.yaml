# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: synerthink-remix-template
metadata:
    template: azd-init@1.5.1
services:
    synerthink-remix-template:
        project: .
        host: containerapp
        language: js
        dist: build
        docker:
            path: Dockerfile
